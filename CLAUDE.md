\# Context

\- Project: Wiki Veilleurs / Ultraplan

\- Stack: HTML/JS (Map), Python (Tools), Git

\- Style: #E0AC60 (Guild Color)



\# Commands

\- Test: `python -m http.server 8765`

\- Check: `curl -s -o /dev/null -w "HTTP %{http\_code}\\n" http://localhost:8765/creator.html`

\- Clean: `pkill -f "http.server 8765"`

\- Git: `git diff Map/map2.html Map/map2.js`



\# Output Rules (TOKEN SAVER)

\- No preamble/postamble (no "I'll help you with...").

\- Code: SNIPPETS ONLY. Use `// ... existing code` for unchanged parts.

\- Never rewrite the whole file unless explicitly asked.

\- Tone: Blunt, technical, critical. 

\- Legal: Cite exact article/source for legal topics; say "Verify in Code" if uncertain.

\- No explanations for obvious logic.

\- Effort: Default /effort low.

